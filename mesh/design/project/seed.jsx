const SEED_NOTES = [
  // Demo trio from PRD
  "TPUs are fast",
  "Matrix multiplication optimization",
  "Neural networks need speed",
  // AI / ML cluster
  "Transformer attention is just weighted dot products",
  "Embeddings turn meaning into geometry",
  "LLMs are compressed libraries of internet text",
  "Inference latency matters more than training time for UX",
  "Small fine-tuned models often beat giant zero-shot ones",
  // Hardware
  "GPU memory bandwidth is the real bottleneck, not flops",
  "Custom silicon makes sense once your workload stops moving",
  // Building / craft
  "Ship the ugly version first, then iterate",
  "A prototype answers questions a spec cannot",
  "Most product ideas die from lack of taste, not lack of code",
  // Design
  "Good UI feels like nothing — bad UI feels like furniture",
  "Negative space is a feature, not leftover canvas",
  "Typography carries 70% of a brand's voice",
  // Thinking / notes
  "I think in fragments, not paragraphs",
  "Ideas need a graph, not a folder",
  "Search by meaning, not by filename",
  "The best notes app is the one you actually open",
  // Focus / process
  "Deep work happens in 90-minute blocks, not 8-hour days",
  "Constraints make the work, freedom kills it",
];

window.SEED_NOTES = SEED_NOTES;
